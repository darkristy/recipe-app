import { NextPage } from "next";

import { withApollo } from "../../lib/withApollo";

const RecipeScreen: NextPage = () => {
	console.log();
	return <div />;
};

export default withApollo(RecipeScreen);
